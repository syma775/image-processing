import cv2
def resize(image,width,height):
    img=cv2.imread(image,1)
    cv2.imshow("orginal color image",img)
    cv2.waitKey(0)
    org_width, org_height = img.shape[0:2]
    print("width:", org_width)
    print("height", org_height)
resize("syma.jpg",0,0)
